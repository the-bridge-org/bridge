{
  "name": "bridge",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream --parallel",
    "develop": "lerna run start --stream --parallel"
  },
  "devDependencies": {
    "lerna": "^3.18.3"
  },
  "license": "MIT"
}
