{
  "name": "bridge",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "develop": "lerna run --parallel start"
  },
  "devDependencies": {
    "lerna": "^3.18.3"
  },
  "license": "MIT"
}
